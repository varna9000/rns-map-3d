[project]
name = "rns-map"
version = "0.1.0"
description = "Reticulum live network map backend"
requires-python = ">=3.9"
dependencies = [
    "rns>=1.1.3",
    "aiohttp>=3.9.5",
    "msgpack>=1.1.2",
]

[project.scripts]
rns-map = "rns_map:main"
